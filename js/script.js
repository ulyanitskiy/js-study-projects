'use strict';

const operandA = Number(prompt('Введите операнд A'));
const operandB = Number(prompt('Введите операнд B'));
const operandC = Number(prompt('Введите операнд C'));
const result = (operandA + operandB + operandC) / 3;

alert(`Среднее арифметическое трёх операндов = ${result}`);